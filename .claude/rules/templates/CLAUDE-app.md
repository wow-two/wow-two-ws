# {repo-name}

## What is this

{One-line description of this application.}

> **This is a community application** — a real product built with the WoW 2.0 ecosystem.

## Key paths

- `src/` — application source
- `tests/` — unit/integration tests
- `docs/` — application documentation
- `README.md` — setup and usage guide

## Build & run

```bash
# Backend
dotnet build
dotnet run

# Frontend (if applicable)
npm install
npm run dev
```

## Architecture

{Brief description of the application's architecture — layers, patterns, key dependencies.}

## Rules

- Use WoW 2.0 platform/sdk packages where applicable
- Follow Clean Architecture conventions
- Commit style: conventional commits (`feat:`, `fix:`, `refactor:`)
