# {repo-name}

## What is this

{One-line description of this SDK package.}

> **This is a public SDK package** — consumed by external users and wow-two-apps projects. Breaking changes require a major version bump.

## Key paths

- `src/` — main source code
- `tests/` — unit/integration tests
- `README.md` — package documentation
- `CHANGELOG.md` — version history

## Build & test

```bash
dotnet build
dotnet test
```

## Package info

- NuGet name: `WoW.Two.Sdk.{Domain}`
- Published: {yes/no}
- Version: {current version}

## Rules

- SemVer strictly enforced — breaking changes = major bump
- All public APIs must have XML doc comments
- Include usage examples in README
- Commit style: conventional commits (`feat:`, `fix:`, `refactor:`)
