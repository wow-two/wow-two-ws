# Capabilities

*Last updated: 2026-09-10*

> A model describing the operations an implementation supports.

## Location

### Folder
- folder placement → [model](model.md).

### File
- file rules → [one type, one file](../../mla.md).

---

## Declaration

### Type doc
- documentation baseline → [model](model.md).

### Construct
- declaration baseline → [model](model.md).

### Type name
- must suffix with `Capabilities`, named for the subject it describes.
- must treat this as a kind of `Model`; it declares supported operations rather than executing them.
