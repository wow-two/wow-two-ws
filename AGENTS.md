# Codex workspace instructions

*Last updated: 2026-09-12*

Personal defaults: `~/.codex/AGENTS.md`. Load `CLAUDE.md`, applicable `.claude/rules/` and `.codex/instructions.md` unless already present in full. Preserve shared Claude sources and local enforcement hooks.

Apply the target repository's own policy across repository boundaries. Staging and unstaging are allowed within the authorized task scope; follow the shared Git conventions. Port and activation details: `.codex/claude-port.md`.
